# --- Terraform ---
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Planes locales (el CI los sube como artefacto, no al repo)
**/tfplan.bin
**/tfplan.txt
**/tfplan.json
*.tfplan
*.bin

# Vars: por defecto ignóralas (sube solo ejemplos no sensibles)
*.tfvars.json
!environments/**/terraform.tfvars.example

# ¡NO ignores el lockfile! Se debe versionar.
# (Asegúrate de NO tener una regla que lo ignore)
# .terraform.lock.hcl

# --- Linters / seguridad (si los usas) ---
.tflint.d/
.tflint.json
.checkov_cache/

# --- Credenciales / llaves (por si acaso) ---
*.pem
*.key
*.p12
*service-account*.json
*credentials*.json

# --- IDE / SO ---
.vscode/
!.vscode/extensions.json
.idea/
*.iml
.DS_Store
Thumbs.db
reports/
environments/packer-dev/terraform-net/modules/auto-stop-idle/function.zip

# --- Policy as Code (Conftest) y seguridad
trivy.deb
conftest.tar.gz
conftest

# --- Python (cache de reglas de checkov)
__pychache__/
*.py[cod]